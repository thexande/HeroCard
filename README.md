#  Hero Card Transition with Auto Layout
This is a demo project using Hero animation library to replicate a card transition from a `UIView` into a `UITableView`''s `tableHeaderView`.
![demo](hero.gif?raw=true "Demo")

